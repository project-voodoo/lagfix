usage: generate_voodoo_initramfs.sh
	-s initramfs_source_directory
	-d voodoo_initramfs_output_directory
	-p voodoo_initramfs_parts_directory
	-x voodoo_lagfix_extensions_directory
	-t stages_source_directory
	[-c] : optional integrated CWM source directory
	[-u] : build full uncompressed initramfs
	[-l] : build full lzma-compressed initramfs
	[-m] : build lzma compressed initramfs without stage3 and sdcard voices
	[-n] : build semi compressed initramfs with some stages and sdcard voices
	[-w] : don't ensure remount as read-only for /system in init.rc

